node_modules/
dist/
.vite/
coverage/
# Playwright E2E artifacts (reports, traces, screenshots) and downloaded browsers
playwright-report/
test-results/
.playwright/
blob-report/
# Claude Code config: ship the shared SessionStart hook + settings, but keep
# personal/local overrides and transient state out of the repo.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json
.DS_Store
*.local
.env
.env.*
!.env.example
.dev.vars
.wrangler/
