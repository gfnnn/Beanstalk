{
  "name": "@beansprout/web",
  "version": "0.1.0",
  "description": "Beansprout.ink marketing site — Vite multi-page static build, deployed to GitHub Pages.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.14"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "vite": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
